[workspace]
resolver = "2"
members = ["utz", "utz-build", "utz-data-nano", "utz-encode", "utz-simplify", "utz-bench-common", "utz-bench-cli"]
# Xtensa target — needs the esp toolchain (espup); built standalone, see its README
exclude = ["utz-bench-firmware"]

[workspace.package]
edition = "2021"
license = "MIT"
repository = "" # TBD

[workspace.dependencies]
geo = "0.32"
geo-types = "0.7"
anyhow = "1"
