[package]
name = "utz"
version = "0.0.0"
edition.workspace = true
license.workspace = true

# Runtime crate — kept lean / no_std-friendly. Codec + build deps added during
# implementation (see PLAN.md §7, §11). geo is a DEV-dependency only (PIP oracle).
[dependencies]

[dev-dependencies]
geo = { workspace = true }
geo-types = { workspace = true }

# [build-dependencies]
# utz-build = { path = "../utz-build" }   # enabled once the GeoJSON pipeline lands
