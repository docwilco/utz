[package]
name = "utz-common"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

[features]
default = ["alloc"]
## The Vec-returning samplers; off leaves the codec vocabulary and LCG core
## for bare-metal consumers.
alloc = []

[dependencies]

[lints]
workspace = true
